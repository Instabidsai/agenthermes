import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Register Your Business | AgentHermes',
  description:
    'Add your business to the AgentHermes verified commerce network. Get audited and become discoverable by AI agents.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
